#version 450
layout(local_size_x = 4) in;
layout(set = 0, binding = 0, std430) buffer Results { uint values[]; };
void main() {
  uint index = gl_GlobalInvocationID.x;
  values[index] = index * 3 + 7;
}
