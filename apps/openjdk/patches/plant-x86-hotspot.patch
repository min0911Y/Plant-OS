--- a/src/hotspot/cpu/x86/macroAssembler_x86.cpp
+++ b/src/hotspot/cpu/x86/macroAssembler_x86.cpp
@@ -2172,6 +2172,13 @@
 
 // A 5 byte nop that is safe for patching (see patch_verified_entry)
 void MacroAssembler::fat_nop() {
+#ifdef __plantos__
+  // A jump to the next instruction keeps the opcode and instruction length
+  // stable when the entry is later redirected. Concurrent decoders must not
+  // combine the old stack-bang opcode with a newly patched displacement.
+  emit_int8(0xe9);
+  emit_int32(0);
+#else
   if (UseAddressNop) {
     addr_nop_5();
   } else {
@@ -2181,6 +2188,7 @@
     emit_int8(0x65); // gs:
     emit_int8((unsigned char)0x90);
   }
+#endif
 }
 
 #ifndef _LP64
--- a/src/hotspot/cpu/x86/c1_MacroAssembler_x86.cpp
+++ b/src/hotspot/cpu/x86/c1_MacroAssembler_x86.cpp
@@ -355,6 +355,9 @@
 
 
 void C1_MacroAssembler::verified_entry(bool breakAtEntry) {
+#ifdef __plantos__
+  fat_nop();
+#else
   if (breakAtEntry || VerifyFPU) {
     // Verified Entry first instruction should be 5 bytes long for correct
     // patching by patch_verified_entry().
@@ -365,6 +368,7 @@
     // For all these cases generate long instruction first.
     fat_nop();
   }
+#endif
   if (breakAtEntry) int3();
   // build frame
   IA32_ONLY( verify_FPU(0, "method_entry"); )
--- a/src/hotspot/cpu/x86/sharedRuntime_x86_64.cpp
+++ b/src/hotspot/cpu/x86/sharedRuntime_x86_64.cpp
@@ -1557,6 +1557,9 @@
     vmIntrinsics::ID iid = method->intrinsic_id();
     intptr_t start = (intptr_t)__ pc();
     int vep_offset = ((intptr_t)__ pc()) - start;
+#ifdef __plantos__
+    __ fat_nop();
+#endif
     gen_special_dispatch(masm,
                          method,
                          in_sig_bt,
@@ -1778,6 +1781,9 @@
   __ bind(hit);
 
   int vep_offset = ((intptr_t)__ pc()) - start;
+#ifdef __plantos__
+  __ fat_nop();
+#endif
 
   if (VM_Version::supports_fast_class_init_checks() && method->needs_clinit_barrier()) {
     Label L_skip_barrier;
--- a/src/hotspot/cpu/x86/x86_64.ad
+++ b/src/hotspot/cpu/x86/x86_64.ad
@@ -885,6 +885,9 @@
   int framesize = C->output()->frame_size_in_bytes();
   int bangsize = C->output()->bang_size_in_bytes();
 
+#ifdef __plantos__
+  __ fat_nop();
+#endif
   if (C->clinit_barrier_on_entry()) {
     assert(VM_Version::supports_fast_class_init_checks(), "sanity");
     assert(!C->method()->holder()->is_not_initialized(), "initialization should have been started");
--- a/src/hotspot/share/c1/c1_Runtime1.cpp
+++ b/src/hotspot/share/c1/c1_Runtime1.cpp
@@ -1201,7 +1201,7 @@
             address ptr = copy_buff + i;
             int a_byte = (*ptr) & 0xFF;
             address dst = instr_pc + i;
-            *(unsigned char*)dst = (unsigned char) a_byte;
+            *code_cache_write_address(dst) = (unsigned char) a_byte;
           }
           ICache::invalidate_range(instr_pc, *byte_count);
           NativeGeneralJump::replace_mt_safe(instr_pc, copy_buff);
--- a/src/hotspot/cpu/x86/assembler_x86.cpp
+++ b/src/hotspot/cpu/x86/assembler_x86.cpp
@@ -10756,7 +10756,7 @@
   assert(src != NULL, "code to copy must have been pre-computed");
   assert(code_section->limit() - code_section->end() > src_len, "code buffer not large enough");
   address end = code_section->end();
-  memcpy(end, src, src_len);
+  memcpy(code_cache_write_address(end), src, src_len);
   code_section->set_end(end + src_len);
 }
 
--- a/src/hotspot/cpu/x86/c1_CodeStubs_x86.cpp
+++ b/src/hotspot/cpu/x86/c1_CodeStubs_x86.cpp
@@ -370,7 +370,7 @@
       address ptr = (address)(_pc_start + i);
       int a_byte = (*ptr) & 0xFF;
       __ emit_int8(a_byte);
-      *ptr = 0x90; // make the site look like a nop
+      *code_cache_write_address(ptr) = 0x90; // make the site look like a nop
     }
   }
 
--- a/src/hotspot/cpu/x86/macroAssembler_x86.hpp
+++ b/src/hotspot/cpu/x86/macroAssembler_x86.hpp
@@ -26,6 +26,7 @@
 #define CPU_X86_MACROASSEMBLER_X86_HPP
 
 #include "asm/assembler.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "code/vmreg.inline.hpp"
 #include "compiler/oopMap.hpp"
 #include "utilities/macros.hpp"
@@ -115,14 +116,15 @@
 
     if (op == 0xEB || (op & 0xF0) == 0x70) {
       // short offset operators (jmp and jcc)
-      char* disp = (char*) &branch[1];
-      int imm8 = target - (address) &disp[1];
+      char* disp = (char*) code_cache_write_address(&branch[1]);
+      int imm8 = target - (address) &branch[2];
       guarantee(this->is8bit(imm8), "Short forward jump exceeds 8-bit offset at %s:%d",
                 file == NULL ? "<NULL>" : file, line);
       *disp = imm8;
     } else {
-      int* disp = (int*) &branch[(op == 0x0F || op == 0xC7)? 2: 1];
-      int imm32 = target - (address) &disp[1];
+      int offset = (op == 0x0F || op == 0xC7) ? 2 : 1;
+      int* disp = (int*) code_cache_write_address(&branch[offset]);
+      int imm32 = target - (address) &branch[offset + 4];
       *disp = imm32;
     }
   }
--- a/src/hotspot/cpu/x86/nativeInst_x86.cpp
+++ b/src/hotspot/cpu/x86/nativeInst_x86.cpp
@@ -127,7 +127,7 @@
 void NativePltCall::set_destination_mt_safe(address dest) {
   // rewriting the value in the GOT, it should always be aligned
   NativeGotJump* jump = nativeGotJump_at(plt_jump());
-  address* got = (address *) jump->got_address();
+  address* got = (address *) code_cache_write_address(jump->got_address());
   *got = dest;
 }
 
@@ -208,8 +208,9 @@
 #ifdef AMD64
   guarantee(disp == (intptr_t)(jint)disp, "must be 32-bit offset");
 #endif // AMD64
-  *code_pos = instruction_code;
-  *((int32_t *)(code_pos+1)) = (int32_t) disp;
+  address writable = code_cache_write_address(code_pos);
+  *writable = instruction_code;
+  *((int32_t *)(writable + 1)) = (int32_t) disp;
   ICache::invalidate_range(code_pos, instruction_size);
 }
 
@@ -224,6 +225,7 @@
 
   NativeCall* n_call =  nativeCall_at (instr_addr); // checking that it is a call
   guarantee((intptr_t)instr_addr % BytesPerWord == 0, "must be aligned");
+  address writable = code_cache_write_address(instr_addr);
 
   // First patch dummy jmp in place
   unsigned char patch[4];
@@ -234,18 +236,18 @@
   patch[3] = 0xFE;
 
   // First patch dummy jmp in place
-  *(jint*)instr_addr = *(jint *)patch;
+  *(jint*)writable = *(jint *)patch;
 
   // Invalidate.  Opteron requires a flush after every write.
   n_call->wrote(0);
 
   // Patch 4th byte
-  instr_addr[4] = code_buffer[4];
+  writable[4] = code_buffer[4];
 
   n_call->wrote(4);
 
   // Patch bytes 0-3
-  *(jint*)instr_addr = *(jint *)code_buffer;
+  *(jint*)writable = *(jint *)code_buffer;
 
   n_call->wrote(0);
 
@@ -468,8 +470,9 @@
   guarantee(disp == (intptr_t)(int32_t)disp, "must be 32-bit offset");
 #endif // AMD64
 
-  *code_pos = instruction_code;
-  *((int32_t*)(code_pos + 1)) = (int32_t)disp;
+  address writable = code_cache_write_address(code_pos);
+  *writable = instruction_code;
+  *((int32_t*)(writable + 1)) = (int32_t)disp;
 
   ICache::invalidate_range(code_pos, instruction_size);
 }
@@ -509,6 +512,9 @@
 // In JVMCI, the restriction is enforced by HotSpotFrameContext.enter(...)
 //
 void NativeJump::patch_verified_entry(address entry, address verified_entry, address dest) {
+#ifdef __plantos__
+  guarantee(*verified_entry == instruction_code, "verified entry must retain its jump opcode");
+#endif
   // complete jump instruction (to be inserted) is in code_buffer;
 #ifdef _LP64
   union {
@@ -524,7 +530,7 @@
   u.code_buffer[0] = instruction_code;
   *(int32_t*)(u.code_buffer + 1) = (int32_t)disp;
 
-  Atomic::store((jlong *) verified_entry, u.cb_long);
+  Atomic::store((jlong *) code_cache_write_address(verified_entry), u.cb_long);
   ICache::invalidate_range(verified_entry, 8);
 
 #else
@@ -537,6 +543,7 @@
 
   // Can't call nativeJump_at() because it's asserts jump exists
   NativeJump* n_jump = (NativeJump*) verified_entry;
+  address writable = code_cache_write_address(verified_entry);
 
   //First patch dummy jmp in place
 
@@ -548,17 +555,17 @@
   patch[3] = 0xFE;
 
   // First patch dummy jmp in place
-  *(int32_t*)verified_entry = *(int32_t *)patch;
+  *(int32_t*)writable = *(int32_t *)patch;
 
   n_jump->wrote(0);
 
   // Patch 5th byte (from jump instruction)
-  verified_entry[4] = code_buffer[4];
+  writable[4] = code_buffer[4];
 
   n_jump->wrote(4);
 
   // Patch bytes 0-3 (from jump instruction)
-  *(int32_t*)verified_entry = *(int32_t *)code_buffer;
+  *(int32_t*)writable = *(int32_t *)code_buffer;
   // Invalidate.  Opteron requires a flush after every write.
   n_jump->wrote(0);
 #endif // _LP64
@@ -582,14 +589,14 @@
 void NativePopReg::insert(address code_pos, Register reg) {
   assert(reg->encoding() < 8, "no space for REX");
   assert(NativePopReg::instruction_size == sizeof(char), "right address unit for update");
-  *code_pos = (u_char)(instruction_code | reg->encoding());
+  *code_cache_write_address(code_pos) = (u_char)(instruction_code | reg->encoding());
   ICache::invalidate_range(code_pos, instruction_size);
 }
 
 
 void NativeIllegalInstruction::insert(address code_pos) {
   assert(NativeIllegalInstruction::instruction_size == sizeof(short), "right address unit for update");
-  *(short *)code_pos = instruction_code;
+  *(short *)code_cache_write_address(code_pos) = instruction_code;
   ICache::invalidate_range(code_pos, instruction_size);
 }
 
@@ -605,8 +612,9 @@
   guarantee(disp == (intptr_t)(int32_t)disp, "must be 32-bit offset");
 #endif // AMD64
 
-  *code_pos = unconditional_long_jump;
-  *((int32_t *)(code_pos+1)) = (int32_t) disp;
+  address writable = code_cache_write_address(code_pos);
+  *writable = unconditional_long_jump;
+  *((int32_t *)(writable + 1)) = (int32_t) disp;
   ICache::invalidate_range(code_pos, instruction_size);
 }
 
@@ -618,6 +626,7 @@
 void NativeGeneralJump::replace_mt_safe(address instr_addr, address code_buffer) {
    assert (instr_addr != NULL, "illegal address for code patching (4)");
    NativeGeneralJump* n_jump =  nativeGeneralJump_at (instr_addr); // checking that it is a jump
+   address writable = code_cache_write_address(instr_addr);
 
    // Temporary code
    unsigned char patch[4];
@@ -628,16 +637,16 @@
    patch[3] = 0xFE;
 
    // First patch dummy jmp in place
-   *(int32_t*)instr_addr = *(int32_t *)patch;
+   *(int32_t*)writable = *(int32_t *)patch;
     n_jump->wrote(0);
 
    // Patch 4th byte
-   instr_addr[4] = code_buffer[4];
+   writable[4] = code_buffer[4];
 
     n_jump->wrote(4);
 
    // Patch bytes 0-3
-   *(jint*)instr_addr = *(jint *)code_buffer;
+   *(jint*)writable = *(jint *)code_buffer;
 
     n_jump->wrote(0);
 
--- a/src/hotspot/cpu/x86/nativeInst_x86.hpp
+++ b/src/hotspot/cpu/x86/nativeInst_x86.hpp
@@ -26,6 +26,7 @@
 #define CPU_X86_NATIVEINST_X86_HPP
 
 #include "asm/assembler.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "runtime/icache.hpp"
 #include "runtime/safepointMechanism.hpp"
 
@@ -82,10 +83,10 @@
   oop  oop_at (int offset) const       { return *(oop*) addr_at(offset); }
 
 
-  void set_char_at(int offset, char c)        { *addr_at(offset) = (u_char)c; wrote(offset); }
-  void set_int_at(int offset, jint  i)        { *(jint*)addr_at(offset) = i;  wrote(offset); }
-  void set_ptr_at (int offset, intptr_t  ptr) { *(intptr_t*) addr_at(offset) = ptr;  wrote(offset); }
-  void set_oop_at (int offset, oop  o)        { *(oop*) addr_at(offset) = o;  wrote(offset); }
+  void set_char_at(int offset, char c)        { *code_cache_write_address(addr_at(offset)) = (u_char)c; wrote(offset); }
+  void set_int_at(int offset, jint  i)        { *(jint*)code_cache_write_address(addr_at(offset)) = i;  wrote(offset); }
+  void set_ptr_at (int offset, intptr_t  ptr) { *(intptr_t*) code_cache_write_address(addr_at(offset)) = ptr;  wrote(offset); }
+  void set_oop_at (int offset, oop  o)        { *(oop*) code_cache_write_address(addr_at(offset)) = o;  wrote(offset); }
 
   // This doesn't really do anything on Intel, but it is the place where
   // cache invalidation belongs, generically:
@@ -459,7 +460,7 @@
   address next_instruction_address() const { return return_address(); }
   intptr_t data() const;
   void set_data(intptr_t data) {
-    intptr_t *addr = (intptr_t *) got_address();
+    intptr_t *addr = (intptr_t *) code_cache_write_address(got_address());
     *addr = data;
   }
 
@@ -611,7 +612,7 @@
 
   address destination() const;
   void set_jump_destination(address dest)  {
-    address *got_entry = (address *) got_address();
+    address *got_entry = (address *) code_cache_write_address(got_address());
     *got_entry = dest;
   }
 
--- a/src/hotspot/cpu/x86/relocInfo_x86.cpp
+++ b/src/hotspot/cpu/x86/relocInfo_x86.cpp
@@ -46,7 +46,7 @@
     if (verify_only) {
       guarantee(*pd_address_in_code() == x, "instructions must match");
     } else {
-      *pd_address_in_code() = x;
+      *(address*)code_cache_write_address((address)pd_address_in_code()) = x;
     }
   } else if (which == Assembler::narrow_oop_operand) {
     address disp = Assembler::locate_operand(addr(), which);
@@ -56,13 +56,13 @@
       if (verify_only) {
         guarantee(*(uint32_t*) disp == encoded, "instructions must match");
       } else {
-        *(int32_t*) disp = encoded;
+        *(int32_t*) code_cache_write_address(disp) = encoded;
       }
     } else {
       if (verify_only) {
         guarantee(*(uint32_t*) disp == CompressedKlassPointers::encode((Klass*)x), "instructions must match");
       } else {
-        *(int32_t*) disp = CompressedKlassPointers::encode((Klass*)x);
+        *(int32_t*) code_cache_write_address(disp) = CompressedKlassPointers::encode((Klass*)x);
       }
     }
   } else {
@@ -73,14 +73,14 @@
     if (verify_only) {
       guarantee(*(int32_t*) disp == (x - next_ip), "instructions must match");
     } else {
-      *(int32_t*) disp = x - next_ip;
+      *(int32_t*) code_cache_write_address(disp) = x - next_ip;
     }
   }
 #else
   if (verify_only) {
     guarantee(*pd_address_in_code() == (x + o), "instructions must match");
   } else {
-    *pd_address_in_code() = x + o;
+    *(address*)code_cache_write_address((address)pd_address_in_code()) = x + o;
   }
 #endif // AMD64
 }
@@ -130,7 +130,7 @@
     // %%%% kludge this, for now, until we get a jump_destination method
     address old_dest = nativeGeneralJump_at(addr())->jump_destination();
     address disp = Assembler::locate_operand(addr(), Assembler::call32_operand);
-    *(jint*)disp += (x - old_dest);
+    *(jint*)code_cache_write_address(disp) += (x - old_dest);
   } else if (ni->is_mov_literal64()) {
     ((NativeMovConstReg*)ni)->set_data((intptr_t)x);
   } else {
--- a/src/hotspot/os/bsd/os_bsd.cpp
+++ b/src/hotspot/os/bsd/os_bsd.cpp
@@ -102,6 +102,11 @@
   #include <mach-o/dyld.h>
 #endif
 
+#if defined(__plantos__)
+extern "C" __attribute__((visibility("hidden"))) void *__dso_handle =
+    &__dso_handle;
+#endif
+
 #ifndef MAP_ANONYMOUS
   #define MAP_ANONYMOUS MAP_ANON
 #endif
@@ -1604,7 +1609,11 @@
 //       left at the time of mmap(). This could be a potential
 //       problem.
 bool os::pd_commit_memory(char* addr, size_t size, bool exec) {
+#if defined(__plantos__)
+  int prot = PROT_READ|PROT_WRITE;
+#else
   int prot = exec ? PROT_READ|PROT_WRITE|PROT_EXEC : PROT_READ|PROT_WRITE;
+#endif
 #if defined(__OpenBSD__)
   // XXX: Work-around mmap/MAP_FIXED bug temporarily on OpenBSD
   Events::log_memprotect(NULL, "Protecting memory [" INTPTR_FORMAT "," INTPTR_FORMAT "] with protection modes %x", p2i(addr), p2i(addr+size), prot);
@@ -1791,6 +1800,7 @@
   case MEM_PROT_NONE: p = PROT_NONE; break;
   case MEM_PROT_READ: p = PROT_READ; break;
   case MEM_PROT_RW:   p = PROT_READ|PROT_WRITE; break;
+  case MEM_PROT_RX:   p = PROT_READ|PROT_EXEC; break;
   case MEM_PROT_RWX:  p = PROT_READ|PROT_WRITE|PROT_EXEC; break;
   default:
     ShouldNotReachHere();
--- a/src/hotspot/os/posix/os_posix.cpp
+++ b/src/hotspot/os/posix/os_posix.cpp
@@ -1197,7 +1197,7 @@
                       "enabled executable stack (see man page execstack(8))");
 
   } else {
-#if !defined(AIX) && !defined(__APPLE__)
+#if defined(LINUX)
     // bsd and aix don't have this
 
     // Accessing stack address below sp may cause SEGV if current
--- a/src/hotspot/os_cpu/bsd_x86/bytes_bsd_x86.hpp
+++ b/src/hotspot/os_cpu/bsd_x86/bytes_bsd_x86.hpp
@@ -34,6 +34,10 @@
 #    define bswap_16(x) OSSwapInt16(x)
 #    define bswap_32(x) OSSwapInt32(x)
 #    define bswap_64(x) OSSwapInt64(x)
+#  elif defined(__plantos__)
+#    define bswap_16(x) __builtin_bswap16(x)
+#    define bswap_32(x) __builtin_bswap32(x)
+#    define bswap_64(x) __builtin_bswap64(x)
 #  elif defined(__OpenBSD__)
 #    define bswap_16(x) swap16(x)
 #    define bswap_32(x) swap32(x)
--- a/src/hotspot/os_cpu/bsd_x86/os_bsd_x86.cpp
+++ b/src/hotspot/os_cpu/bsd_x86/os_bsd_x86.cpp
@@ -63,6 +63,9 @@
 # include <stdio.h>
 # include <unistd.h>
 # include <sys/resource.h>
+#ifdef __plantos__
+# include <sys/sysctl.h>
+#endif
 # include <sys/stat.h>
 # include <sys/time.h>
 # include <sys/utsname.h>
@@ -74,7 +77,7 @@
 # include <ucontext.h>
 #endif
 
-#if !defined(__APPLE__) && !defined(__NetBSD__)
+#if !defined(__APPLE__) && !defined(__NetBSD__) && !defined(__plantos__)
 # include <pthread_np.h>
 #endif
 
@@ -95,6 +98,32 @@
 #define SPELL_REG_FP "ebp"
 #endif // AMD64
 
+#ifdef __plantos__
+# define context_rip uc_mcontext.rip
+# define context_rsp uc_mcontext.rsp
+# define context_rbp uc_mcontext.rbp
+# define context_rax uc_mcontext.rax
+# define context_rbx uc_mcontext.rbx
+# define context_rcx uc_mcontext.rcx
+# define context_rdx uc_mcontext.rdx
+# define context_rsi uc_mcontext.rsi
+# define context_rdi uc_mcontext.rdi
+# define context_r8 uc_mcontext.r8
+# define context_r9 uc_mcontext.r9
+# define context_r10 uc_mcontext.r10
+# define context_r11 uc_mcontext.r11
+# define context_r12 uc_mcontext.r12
+# define context_r13 uc_mcontext.r13
+# define context_r14 uc_mcontext.r14
+# define context_r15 uc_mcontext.r15
+# define context_pc context_rip
+# define context_sp context_rsp
+# define context_fp context_rbp
+# define context_flags uc_mcontext.rflags
+# define context_err uc_mcontext.error
+# define context_trapno uc_mcontext.vector
+#endif
+
 #ifdef __FreeBSD__
 # define context_trapno uc_mcontext.mc_trapno
 # ifdef AMD64
@@ -763,13 +792,21 @@
   if (rslt != 0)
     fatal("pthread_attr_init failed with error = %d", rslt);
 
+#ifdef __plantos__
+  rslt = pthread_getattr_np(pthread_self(), &attr);
+#else
   rslt = pthread_attr_get_np(pthread_self(), &attr);
+#endif
 
   if (rslt != 0)
     fatal("pthread_attr_get_np failed with error = %d", rslt);
 
+#ifdef __plantos__
+  if (pthread_attr_getstack(&attr, (void **)bottom, size) != 0) {
+#else
   if (pthread_attr_getstackaddr(&attr, (void **)bottom) != 0 ||
     pthread_attr_getstacksize(&attr, size) != 0) {
+#endif
     fatal("Can not locate current stack attributes!");
   }
 
--- a/src/hotspot/os_cpu/bsd_zero/os_bsd_zero.cpp
+++ b/src/hotspot/os_cpu/bsd_zero/os_bsd_zero.cpp
@@ -57,11 +57,6 @@
 #include "utilities/events.hpp"
 #include "utilities/vmError.hpp"
 
-#if defined(__plantos__)
-extern "C" __attribute__((visibility("hidden"))) void *__dso_handle =
-    &__dso_handle;
-#endif
-
 address os::current_stack_pointer() {
   return (address)__builtin_frame_address(0);
 }
--- a/src/hotspot/share/asm/codeBuffer.cpp
+++ b/src/hotspot/share/asm/codeBuffer.cpp
@@ -647,10 +647,10 @@
       if (buf_offset % HeapWordSize == 0) {
         // Use wordwise copies if possible:
         Copy::disjoint_words((HeapWord*)lstart,
-                             (HeapWord*)(buf+buf_offset),
+                             (HeapWord*)code_cache_write_address(buf+buf_offset),
                              (lsize + HeapWordSize-1) / HeapWordSize);
       } else {
-        Copy::conjoint_jbytes(lstart, buf+buf_offset, lsize);
+        Copy::conjoint_jbytes(lstart, code_cache_write_address(buf+buf_offset), lsize);
       }
     }
     buf_offset += lsize;
@@ -661,7 +661,7 @@
     if (buf != NULL) {
       relocInfo padding = relocInfo(relocInfo::none, 0);
       assert(buf_offset + (csize_t)sizeof(padding) <= buf_limit, "padding in bounds");
-      *(relocInfo*)(buf+buf_offset) = padding;
+      *(relocInfo*)code_cache_write_address(buf+buf_offset) = padding;
     }
     buf_offset += sizeof(relocInfo);
   }
@@ -735,13 +735,13 @@
     // Copy the code as aligned machine words.
     // This may also include an uninitialized partial word at the end.
     Copy::disjoint_words((HeapWord*)cs->start(),
-                         (HeapWord*)dest_cs->start(),
+                         (HeapWord*)code_cache_write_address(dest_cs->start()),
                          wsize / HeapWordSize);
 
     if (dest->blob() == NULL) {
       // Destination is a final resting place, not just another buffer.
       // Normalize uninitialized bytes in the final padding.
-      Copy::fill_to_bytes(dest_cs->end(), dest_cs->remaining(),
+      Copy::fill_to_bytes(code_cache_write_address(dest_cs->end()), dest_cs->remaining(),
                           Assembler::code_fill_byte());
     }
     // Keep track of the highest filled address
@@ -775,7 +775,7 @@
   if (dest->blob() == NULL && dest_filled != NULL) {
     // Destination is a final resting place, not just another buffer.
     // Normalize uninitialized bytes in the final padding.
-    Copy::fill_to_bytes(dest_filled, dest_end - dest_filled,
+    Copy::fill_to_bytes(code_cache_write_address(dest_filled), dest_end - dest_filled,
                         Assembler::code_fill_byte());
 
   }
@@ -905,7 +905,7 @@
   cb.set_blob(NULL);
 
   // Zap the old code buffer contents, to avoid mistakenly using them.
-  debug_only(Copy::fill_to_bytes(bxp->_total_start, bxp->_total_size,
+  debug_only(Copy::fill_to_bytes(code_cache_write_address(bxp->_total_start), bxp->_total_size,
                                  badCodeHeapFreeVal);)
 
   // Make certain that the new sections are all snugly inside the new blob.
--- a/src/hotspot/share/asm/codeBuffer.hpp
+++ b/src/hotspot/share/asm/codeBuffer.hpp
@@ -26,6 +26,7 @@
 #define SHARE_ASM_CODEBUFFER_HPP
 
 #include "code/oopRecorder.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "code/relocInfo.hpp"
 #include "utilities/align.hpp"
 #include "utilities/debug.hpp"
@@ -201,45 +202,48 @@
   // Code emission
   void emit_int8(int8_t x1) {
     address curr = end();
-    *((int8_t*)  curr++) = x1;
+    *((int8_t*) code_cache_write_address(curr++)) = x1;
     set_end(curr);
   }
 
-  void emit_int16(int16_t x) { *((int16_t*) end()) = x; set_end(end() + sizeof(int16_t)); }
+  void emit_int16(int16_t x) { *((int16_t*) code_cache_write_address(end())) = x; set_end(end() + sizeof(int16_t)); }
   void emit_int16(int8_t x1, int8_t x2) {
     address curr = end();
-    *((int8_t*)  curr++) = x1;
-    *((int8_t*)  curr++) = x2;
+    address writable = code_cache_write_address(curr);
+    *((int8_t*) writable++) = x1;
+    *((int8_t*) writable++) = x2;
     set_end(curr + 2);
   }
 
   void emit_int24(int8_t x1, int8_t x2, int8_t x3)  {
     address curr = end();
-    *((int8_t*)  curr++) = x1;
-    *((int8_t*)  curr++) = x2;
-    *((int8_t*)  curr++) = x3;
+    address writable = code_cache_write_address(curr);
+    *((int8_t*) writable++) = x1;
+    *((int8_t*) writable++) = x2;
+    *((int8_t*) writable++) = x3;
     set_end(curr + 3);
   }
 
   void emit_int32(int32_t x) {
     address curr = end();
-    *((int32_t*) curr) = x;
+    *((int32_t*) code_cache_write_address(curr)) = x;
     set_end(curr + sizeof(int32_t));
   }
   void emit_int32(int8_t x1, int8_t x2, int8_t x3, int8_t x4)  {
     address curr = end();
-    *((int8_t*)  curr++) = x1;
-    *((int8_t*)  curr++) = x2;
-    *((int8_t*)  curr++) = x3;
-    *((int8_t*)  curr++) = x4;
+    address writable = code_cache_write_address(curr);
+    *((int8_t*) writable++) = x1;
+    *((int8_t*) writable++) = x2;
+    *((int8_t*) writable++) = x3;
+    *((int8_t*) writable++) = x4;
     set_end(curr + 4);
   }
 
-  void emit_int64( int64_t x)  { *((int64_t*) end()) = x; set_end(end() + sizeof(int64_t)); }
+  void emit_int64( int64_t x)  { *((int64_t*) code_cache_write_address(end())) = x; set_end(end() + sizeof(int64_t)); }
 
-  void emit_float( jfloat  x)  { *((jfloat*)  end()) = x; set_end(end() + sizeof(jfloat)); }
-  void emit_double(jdouble x)  { *((jdouble*) end()) = x; set_end(end() + sizeof(jdouble)); }
-  void emit_address(address x) { *((address*) end()) = x; set_end(end() + sizeof(address)); }
+  void emit_float( jfloat  x)  { *((jfloat*)  code_cache_write_address(end())) = x; set_end(end() + sizeof(jfloat)); }
+  void emit_double(jdouble x)  { *((jdouble*) code_cache_write_address(end())) = x; set_end(end() + sizeof(jdouble)); }
+  void emit_address(address x) { *((address*) code_cache_write_address(end())) = x; set_end(end() + sizeof(address)); }
 
   // Share a scratch buffer for relocinfo.  (Hacky; saves a resource allocation.)
   void initialize_shared_locs(relocInfo* buf, int length);
--- a/src/hotspot/share/code/codeBlob.cpp
+++ b/src/hotspot/share/code/codeBlob.cpp
@@ -44,6 +44,7 @@
 #include "runtime/javaFrameAnchor.hpp"
 #include "runtime/jniHandles.hpp"
 #include "runtime/mutexLocker.hpp"
+#include "runtime/os.hpp"
 #include "runtime/safepoint.hpp"
 #include "runtime/sharedRuntime.hpp"
 #include "runtime/stubCodeGenerator.hpp"
@@ -60,9 +61,13 @@
 }
 
 unsigned int CodeBlob::align_code_offset(int offset) {
-  // align the size to CodeEntryAlignment
+#if defined(__plantos__)
+  const int alignment = os::vm_page_size();
+#else
+  const int alignment = CodeEntryAlignment;
+#endif
   int header_size = (int)CodeHeap::header_size();
-  return align_up(offset + header_size, CodeEntryAlignment) - header_size;
+  return align_up(offset + header_size, alignment) - header_size;
 }
 
 
@@ -73,6 +78,9 @@
   // align the size to CodeEntryAlignment
   size = align_code_offset(size);
   size += align_up(cb->total_content_size(), oopSize);
+#if defined(__plantos__)
+  size = align_code_offset(size);
+#endif
   size += align_up(cb->total_oop_size(), oopSize);
   size += align_up(cb->total_metadata_size(), oopSize);
   return size;
@@ -182,6 +190,7 @@
   assert(!CodeCache_lock->owned_by_self(), "release CodeCache before registering the stub");
 
   if (stub != NULL) {
+    CodeCache::make_executable(stub);
     char stub_id[256];
     assert(strlen(name1) + strlen(name2) < sizeof(stub_id), "");
     jio_snprintf(stub_id, sizeof(stub_id), "%s%s", name1, name2);
@@ -242,6 +251,7 @@
   {
     MutexLocker mu(CodeCache_lock, Mutex::_no_safepoint_check_flag);
     blob = new (size) BufferBlob(name, size);
+    if (blob != NULL) CodeCache::make_executable(blob);
   }
   // Track memory usage statistic after releasing CodeCache_lock
   MemoryService::track_code_cache_memory_usage();
@@ -263,6 +273,7 @@
   {
     MutexLocker mu(CodeCache_lock, Mutex::_no_safepoint_check_flag);
     blob = new (size) BufferBlob(name, size, cb);
+    if (blob != NULL) CodeCache::make_executable(blob);
   }
   // Track memory usage statistic after releasing CodeCache_lock
   MemoryService::track_code_cache_memory_usage();
@@ -303,6 +314,7 @@
   {
     MutexLocker mu(CodeCache_lock, Mutex::_no_safepoint_check_flag);
     blob = new (size) AdapterBlob(size, cb);
+    if (blob != NULL) CodeCache::make_executable(blob);
   }
   // Track memory usage statistic after releasing CodeCache_lock
   MemoryService::track_code_cache_memory_usage();
@@ -348,6 +360,7 @@
       return NULL;
     }
     blob = new (size) VtableBlob(name, size);
+    if (blob != NULL) CodeCache::make_executable(blob);
     CodeCache_lock->unlock();
   }
   // Track memory usage statistic after releasing CodeCache_lock
@@ -370,6 +383,7 @@
   {
     MutexLocker mu(CodeCache_lock, Mutex::_no_safepoint_check_flag);
     blob = new (size) MethodHandlesAdapterBlob(size);
+    if (blob != NULL) CodeCache::make_executable(blob);
     if (blob == NULL) {
       vm_exit_out_of_memory(size, OOM_MALLOC_ERROR, "CodeCache: no room for method handle adapter blob");
     }
@@ -739,6 +753,7 @@
   {
     MutexLocker mu(CodeCache_lock, Mutex::_no_safepoint_check_flag);
     blob = new (size) OptimizedEntryBlob(name, size, cb, exception_handler_offset, receiver, frame_data_offset);
+    if (blob != NULL) CodeCache::make_executable(blob);
   }
   // Track memory usage statistic after releasing CodeCache_lock
   MemoryService::track_code_cache_memory_usage();
--- a/src/hotspot/share/code/codeBlob.hpp
+++ b/src/hotspot/share/code/codeBlob.hpp
@@ -318,7 +318,11 @@
     _relocation_size(align_up(cb->total_relocation_size(), oopSize)),
     _content_offset(CodeBlob::align_code_offset(_header_size + _relocation_size)),
     _code_offset(_content_offset + cb->total_offset_of(cb->insts())),
+#if defined(__plantos__)
+    _data_offset(CodeBlob::align_code_offset(_content_offset + align_up(cb->total_content_size(), oopSize)))
+#else
     _data_offset(_content_offset + align_up(cb->total_content_size(), oopSize))
+#endif
   {
     assert(is_aligned(_relocation_size, oopSize), "unaligned size");
 
--- a/src/hotspot/share/code/codeCache.cpp
+++ b/src/hotspot/share/code/codeCache.cpp
@@ -26,6 +26,7 @@
 #include "jvm_io.h"
 #include "code/codeBlob.hpp"
 #include "code/codeCache.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "code/codeHeapState.hpp"
 #include "code/compiledIC.hpp"
 #include "code/dependencies.hpp"
@@ -167,6 +168,12 @@
 int CodeCache::_number_of_nmethods_with_dependencies = 0;
 ExceptionCache* volatile CodeCache::_exception_cache_purge_list = NULL;
 
+#ifdef __plantos__
+address code_cache_write_address(address source) {
+  CodeHeap* heap = CodeCache::get_code_heap_containing(source);
+  return heap != NULL ? heap->write_address(source) : source;
+}
+#endif
 // Initialize arrays of CodeHeap subsets
 GrowableArray<CodeHeap*>* CodeCache::_heaps = new(ResourceObj::C_HEAP, mtCode) GrowableArray<CodeHeap*> (CodeBlobType::All, mtCode);
 GrowableArray<CodeHeap*>* CodeCache::_compiled_heaps = new(ResourceObj::C_HEAP, mtCode) GrowableArray<CodeHeap*> (CodeBlobType::All, mtCode);
@@ -584,8 +591,29 @@
   return cb;
 }
 
+void CodeCache::make_executable(CodeBlob* cb) {
+#if defined(__plantos__)
+  CodeHeap* heap = get_code_heap(cb);
+  guarantee(heap != NULL && heap->make_executable(cb),
+            "cannot make code cache content executable");
+#else
+  (void)cb;
+#endif
+}
+
+void CodeCache::make_writable(CodeBlob* cb) {
+#if defined(__plantos__)
+  CodeHeap* heap = get_code_heap(cb);
+  guarantee(heap != NULL && heap->make_writable(cb),
+            "cannot make code cache block writable");
+#else
+  (void)cb;
+#endif
+}
+
 void CodeCache::free(CodeBlob* cb) {
   assert_locked_or_safepoint(CodeCache_lock);
+  make_writable(cb);
   CodeHeap* heap = get_code_heap(cb);
   print_trace("free", cb);
   if (cb->is_nmethod()) {
--- a/src/hotspot/share/code/codeCache.hpp
+++ b/src/hotspot/share/code/codeCache.hpp
@@ -78,6 +78,7 @@
 class ShenandoahParallelCodeHeapIterator;
 
 class CodeCache : AllStatic {
+  friend address code_cache_write_address(address source);
   friend class VMStructs;
   friend class JVMCIVMStructs;
   template <class T, class Filter> friend class CodeBlobIterator;
@@ -138,6 +139,8 @@
   // Allocation/administration
   static CodeBlob* allocate(int size, int code_blob_type, bool handle_alloc_failure = true, int orig_code_blob_type = CodeBlobType::All); // allocates a new CodeBlob
   static void commit(CodeBlob* cb);                        // called when the allocated CodeBlob has been filled
+  static void make_executable(CodeBlob* cb);
+  static void make_writable(CodeBlob* cb);
   static int  alignment_unit();                            // guaranteed alignment of all CodeBlobs
   static int  alignment_offset();                          // guaranteed offset of first CodeBlob byte within alignment unit (i.e., allocation header)
   static void free(CodeBlob* cb);                          // frees a CodeBlob
--- a/src/hotspot/share/code/codeCacheWrite.hpp
+++ b/src/hotspot/share/code/codeCacheWrite.hpp
@@ -0,0 +1,22 @@
+/*
+ * Copyright (c) 2026, Plant OS contributors. All rights reserved.
+ *
+ * This code is free software; you can redistribute it and/or modify it
+ * under the terms of the GNU General Public License version 2 only, as
+ * published by the Free Software Foundation.
+ */
+
+#ifndef SHARE_CODE_CODECACHEWRITE_HPP
+#define SHARE_CODE_CODECACHEWRITE_HPP
+
+#include "utilities/globalDefinitions.hpp"
+
+#ifdef __plantos__
+address code_cache_write_address(address source);
+#else
+inline address code_cache_write_address(address source) {
+  return source;
+}
+#endif
+
+#endif // SHARE_CODE_CODECACHEWRITE_HPP
--- a/src/hotspot/share/code/icBuffer.cpp
+++ b/src/hotspot/share/code/icBuffer.cpp
@@ -106,7 +106,8 @@
   // We cannot store a pointer to the 'ic' object, since it is resource allocated. Instead we
   // store the location of the inline cache. Then we have enough information recreate the CompiledIC
   // object when we need to remove the stub.
-  _ic_site = ic->instruction_address();
+  ((ICStub*)code_cache_write_address((address)this))->_ic_site =
+      ic->instruction_address();
 
   // Assemble new stub
   InlineCacheBuffer::assemble_ic_buffer_code(code_begin(), cached_val, dest_addr);
@@ -119,7 +120,7 @@
   if (CompiledIC::is_icholder_entry(destination())) {
     InlineCacheBuffer::queue_for_release((CompiledICHolder*)cached_value());
   }
-  _ic_site = NULL;
+  ((ICStub*)code_cache_write_address((address)this))->_ic_site = NULL;
 }
 
 
--- a/src/hotspot/share/code/icBuffer.hpp
+++ b/src/hotspot/share/code/icBuffer.hpp
@@ -26,6 +26,7 @@
 #define SHARE_CODE_ICBUFFER_HPP
 
 #include "asm/codeBuffer.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "code/stubs.hpp"
 #include "interpreter/bytecodes.hpp"
 #include "memory/allocation.hpp"
@@ -56,7 +57,11 @@
  protected:
   friend class ICStubInterface;
   // This will be called only by ICStubInterface
-  void    initialize(int size)                   { _size = size; _ic_site = NULL; }
+  void    initialize(int size) {
+    ICStub* writable = (ICStub*)code_cache_write_address((address)this);
+    writable->_size = size;
+    writable->_ic_site = NULL;
+  }
   void    finalize(); // called when a method is removed
 
   // General info
--- a/src/hotspot/share/code/nmethod.cpp
+++ b/src/hotspot/share/code/nmethod.cpp
@@ -704,6 +704,7 @@
     debug_only(Universe::heap()->verify_nmethod(this));
 
     CodeCache::commit(this);
+    CodeCache::make_executable(this);
   }
 
   if (PrintNativeNMethods || PrintDebugInfo || PrintRelocations || PrintDependencies) {
@@ -900,6 +901,8 @@
     }
 #endif
 
+    CodeCache::make_executable(this);
+
     // we use the information of entry points to find out if a method is
     // static or non static
     assert(compiler->is_c2() || compiler->is_jvmci() ||
@@ -1914,7 +1917,7 @@
                (r->oop_addr() >= oops_begin() && r->oop_addr() < oops_end()),
                "oop must be found in exactly one place");
         if (r->oop_is_immediate() && r->oop_value() != NULL) {
-          f->do_oop(r->oop_addr());
+          f->do_oop((oop*)code_cache_write_address((address)r->oop_addr()));
         }
       }
     }
--- a/src/hotspot/share/code/relocInfo.cpp
+++ b/src/hotspot/share/code/relocInfo.cpp
@@ -27,6 +27,7 @@
 #include "code/compiledIC.hpp"
 #include "code/nmethod.hpp"
 #include "code/relocInfo.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "memory/resourceArea.hpp"
 #include "memory/universe.hpp"
 #include "oops/compressedOops.inline.hpp"
@@ -278,10 +279,10 @@
 void Relocation::const_set_data_value(address x) {
 #ifdef _LP64
   if (format() == relocInfo::narrow_oop_in_const) {
-    *(narrowOop*)addr() = CompressedOops::encode(cast_to_oop(x));
+    *(narrowOop*)code_cache_write_address(addr()) = CompressedOops::encode(cast_to_oop(x));
   } else {
 #endif
-    *(address*)addr() = x;
+    *(address*)code_cache_write_address(addr()) = x;
 #ifdef _LP64
   }
 #endif
--- a/src/hotspot/share/code/vtableStubs.hpp
+++ b/src/hotspot/share/code/vtableStubs.hpp
@@ -26,6 +26,7 @@
 #define SHARE_CODE_VTABLESTUBS_HPP
 
 #include "asm/macroAssembler.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "code/vmreg.hpp"
 #include "memory/allocation.hpp"
 
@@ -122,7 +123,7 @@
   static VMReg   _receiver_location; // Where to find receiver
 
   VtableStub*    _next;              // Pointer to next entry in hash table
-  const short    _index;             // vtable index
+  short          _index;             // vtable index
   short          _ame_offset;        // Where an AbstractMethodError might occur
   short          _npe_offset;        // Where a NullPointerException might occur
   bool           _is_vtable_stub;    // True if vtable stub, false, is itable stub
@@ -130,13 +131,21 @@
 
   void* operator new(size_t size, int code_size) throw();
 
-  VtableStub(bool is_vtable_stub, int index)
-        : _next(NULL), _index(index), _ame_offset(-1), _npe_offset(-1),
-          _is_vtable_stub(is_vtable_stub) {}
+  VtableStub(bool is_vtable_stub, int index) {
+    VtableStub* writable =
+        (VtableStub*)code_cache_write_address((address)this);
+    writable->_next = NULL;
+    writable->_index = (short)index;
+    writable->_ame_offset = -1;
+    writable->_npe_offset = -1;
+    writable->_is_vtable_stub = is_vtable_stub;
+  }
   VtableStub* next() const                       { return _next; }
   int index() const                              { return _index; }
   static VMReg receiver_location()               { return _receiver_location; }
-  void set_next(VtableStub* n)                   { _next = n; }
+  void set_next(VtableStub* n) {
+    ((VtableStub*)code_cache_write_address((address)this))->_next = n;
+  }
 
  public:
   address code_begin() const                     { return (address)(this + 1); }
@@ -151,8 +160,10 @@
 
  private:
   void set_exception_points(address npe_addr, address ame_addr) {
-    _npe_offset = npe_addr - code_begin();
-    _ame_offset = ame_addr - code_begin();
+    VtableStub* writable =
+        (VtableStub*)code_cache_write_address((address)this);
+    writable->_npe_offset = npe_addr - code_begin();
+    writable->_ame_offset = ame_addr - code_begin();
     assert(is_abstract_method_error(ame_addr),   "offset must be correct");
     assert(is_null_pointer_exception(npe_addr),  "offset must be correct");
     assert(!is_abstract_method_error(npe_addr),  "offset must be correct");
--- a/src/hotspot/share/interpreter/interpreter.cpp
+++ b/src/hotspot/share/interpreter/interpreter.cpp
@@ -51,13 +51,15 @@
 // Implementation of InterpreterCodelet
 
 void InterpreterCodelet::initialize(const char* description, Bytecodes::Code bytecode) {
-  _description = description;
-  _bytecode    = bytecode;
+  InterpreterCodelet* writable =
+      (InterpreterCodelet*)code_cache_write_address((address)this);
+  writable->_description = description;
+  writable->_bytecode    = bytecode;
 #ifndef PRODUCT
-  AsmRemarks* arp = new(&_asm_remarks) AsmRemarks();
-  DbgStrings* dsp = new(&_dbg_strings) DbgStrings();
-  postcond(arp == &_asm_remarks);
-  postcond(dsp == &_dbg_strings);
+  AsmRemarks* arp = new(&writable->_asm_remarks) AsmRemarks();
+  DbgStrings* dsp = new(&writable->_dbg_strings) DbgStrings();
+  postcond(arp == &writable->_asm_remarks);
+  postcond(dsp == &writable->_dbg_strings);
 #endif
 }
 
--- a/src/hotspot/share/interpreter/interpreter.hpp
+++ b/src/hotspot/share/interpreter/interpreter.hpp
@@ -26,6 +26,7 @@
 #define SHARE_INTERPRETER_INTERPRETER_HPP
 
 #include "code/stubs.hpp"
+#include "code/codeCacheWrite.hpp"
 #include "interpreter/interp_masm.hpp"
 #include "interpreter/templateInterpreter.hpp"
 #include "interpreter/zero/zeroInterpreter.hpp"
@@ -54,7 +55,11 @@
 
  public:
   // Initialization/finalization
-  void    initialize(int size)                   { _size = size; }
+  void    initialize(int size) {
+    InterpreterCodelet* writable =
+        (InterpreterCodelet*)code_cache_write_address((address)this);
+    writable->_size = size;
+  }
   void    finalize()                             { ShouldNotCallThis(); }
 
   // General info/converters
@@ -83,8 +88,12 @@
     // nor are they ever finalized (see above).
     ShouldNotCallThis();
   }
-  void use_remarks(AsmRemarks &remarks) { _asm_remarks.share(remarks); }
-  void use_strings(DbgStrings &strings) { _dbg_strings.share(strings); }
+  void use_remarks(AsmRemarks &remarks) {
+    ((InterpreterCodelet*)code_cache_write_address((address)this))->_asm_remarks.share(remarks);
+  }
+  void use_strings(DbgStrings &strings) {
+    ((InterpreterCodelet*)code_cache_write_address((address)this))->_dbg_strings.share(strings);
+  }
 #endif
 };
 
--- a/src/hotspot/share/interpreter/interpreterRuntime.cpp
+++ b/src/hotspot/share/interpreter/interpreterRuntime.cpp
@@ -1299,7 +1299,7 @@
     handler = set_handler_blob();
   }
   if (handler != NULL) {
-    memcpy(handler, buffer->insts_begin(), insts_size);
+    memcpy(code_cache_write_address(handler), buffer->insts_begin(), insts_size);
     pd_set_handler(handler);
     ICache::invalidate_range(handler, insts_size);
     _handler = handler + insts_size;
--- a/src/hotspot/share/memory/heap.cpp
+++ b/src/hotspot/share/memory/heap.cpp
@@ -24,6 +24,10 @@
 
 #include "precompiled.hpp"
 #include "memory/heap.hpp"
+#include "code/codeCacheWrite.hpp"
+#ifdef __plantos__
+#include <vm.h>
+#endif
 #include "oops/oop.inline.hpp"
 #include "runtime/os.hpp"
 #include "runtime/mutexLocker.hpp"
@@ -51,6 +55,9 @@
   _adapter_count                = 0;
   _full_count                   = 0;
   _fragmentation_count          = 0;
+#ifdef __plantos__
+  _write_base                   = NULL;
+#endif
 }
 
 // Dummy initialization of template array.
@@ -190,12 +197,56 @@
 
 
 void CodeHeap::on_code_mapping(char* base, size_t size) {
+#ifdef __plantos__
+  if (_write_base == NULL) {
+    _write_base = (address)os::reserve_memory(_memory.reserved_size());
+    guarantee(_write_base != NULL, "cannot reserve code cache write alias");
+  }
+  // Reserve once per heap so instructions and bulk copies may cross commits.
+  guarantee(vm_map_alias(base, write_address((address)base), size) != NULL,
+            "cannot commit code cache write alias");
+#endif
 #ifdef LINUX
   extern void linux_wrap_code(char* base, size_t size);
   linux_wrap_code(base, size);
 #endif
 }
 
+bool CodeHeap::make_executable(CodeBlob* blob) {
+#if defined(__plantos__)
+  if (blob == NULL || !contains_blob(blob)) {
+    return false;
+  }
+  char* begin = (char*)blob->content_begin();
+  char* end = (char*)blob->content_end();
+  if (begin >= end) {
+    return true;
+  }
+  const size_t page_size = (size_t)os::vm_page_size();
+  assert(((uintptr_t)begin & (page_size - 1)) == 0,
+         "code content must start at a page boundary");
+  assert(((uintptr_t)end & (page_size - 1)) == 0,
+         "code content must end at a page boundary");
+  return os::protect_memory(begin, end - begin, os::MEM_PROT_RX);
+#else
+  (void)blob;
+  return true;
+#endif
+}
+
+bool CodeHeap::make_writable(void* p) {
+#if defined(__plantos__)
+  HeapBlock* block = (HeapBlock*)find_block_for(p);
+  if (block == NULL || block->free()) {
+    return false;
+  }
+  return os::protect_memory((char*)block, segments_to_size(block->length()),
+                            os::MEM_PROT_RW);
+#else
+  (void)p;
+  return true;
+#endif
+}
 
 bool CodeHeap::reserve(ReservedSpace rs, size_t committed_size, size_t segment_size) {
   assert(rs.size() >= committed_size, "reserved < committed");
@@ -281,6 +332,10 @@
   assert_locked_or_safepoint(CodeCache_lock);
 
   // First check if we can satisfy request from freelist
+#ifdef __plantos__
+  const size_t page_segments = (size_t)os::vm_page_size() >> _log2_segment_size;
+  number_of_segments = align_up(number_of_segments, page_segments);
+#endif
   NOT_PRODUCT(verify());
   HeapBlock* block = search_freelist(number_of_segments);
   NOT_PRODUCT(verify());
@@ -298,6 +353,9 @@
 
   // Ensure minimum size for allocation to the heap.
   number_of_segments = MAX2((int)CodeCacheMinBlockLength, (int)number_of_segments);
+#ifdef __plantos__
+  number_of_segments = align_up(number_of_segments, page_segments);
+#endif
 
   if (_next_segment + number_of_segments <= _number_of_committed_segments) {
     mark_segmap_as_used(_next_segment, _next_segment + number_of_segments, false);
@@ -351,9 +409,22 @@
 
   size_t actual_number_of_segments = b->length();
   size_t used_number_of_segments   = size_to_segments(used_size + header_size());
+#ifdef __plantos__
+  const size_t page_segments = (size_t)os::vm_page_size() >> _log2_segment_size;
+  used_number_of_segments = align_up(used_number_of_segments, page_segments);
+#endif
   size_t unused_number_of_segments = actual_number_of_segments - used_number_of_segments;
   guarantee(used_number_of_segments <= actual_number_of_segments, "Must be!");
 
+#ifdef __plantos__
+  if (unused_number_of_segments != 0) {
+    char* tail = (char*)block_at(segment_for(b) + used_number_of_segments);
+    guarantee(os::protect_memory(tail,
+                                 segments_to_size(unused_number_of_segments),
+                                 os::MEM_PROT_RW),
+              "cannot make freed code cache tail writable");
+  }
+#endif
   HeapBlock* f = split_block(b, used_number_of_segments);
   add_to_freelist(f);
   NOT_PRODUCT(verify());
@@ -365,6 +436,9 @@
 
   // Find start of HeapBlock
   HeapBlock* b = (((HeapBlock *)p) - 1);
+#ifdef __plantos__
+  guarantee(make_writable(b), "cannot make code cache block writable");
+#endif
   assert(b->allocated_space() == p, "sanity check");
   guarantee((char*) b >= _memory.low_boundary() && (char*) b < _memory.high(),
             "The block to be deallocated " INTPTR_FORMAT " is not within the heap "
--- a/src/hotspot/share/memory/heap.hpp
+++ b/src/hotspot/share/memory/heap.hpp
@@ -80,8 +80,12 @@
 
 class CodeHeap : public CHeapObj<mtCode> {
   friend class VMStructs;
+  friend class CodeCache;
  protected:
   VirtualSpace _memory;                          // the memory holding the blocks
+#ifdef __plantos__
+  address      _write_base;                      // contiguous alias of the reserved code heap
+#endif
   VirtualSpace _segmap;                          // the memory holding the segment map
 
   size_t       _number_of_committed_segments;
@@ -143,9 +147,16 @@
 
   // to perform additional actions on creation of executable code
   void on_code_mapping(char* base, size_t size);
+  bool make_executable(CodeBlob* blob);
+  bool make_writable(void* p);
 
  public:
   CodeHeap(const char* name, const int code_blob_type);
+#ifdef __plantos__
+  address write_address(address source) const {
+    return _write_base + (source - (address)_memory.low_boundary());
+  }
+#endif
 
   // Heap extents
   bool  reserve(ReservedSpace rs, size_t committed_size, size_t segment_size);
--- a/src/hotspot/share/runtime/icache.cpp
+++ b/src/hotspot/share/runtime/icache.cpp
@@ -32,6 +32,9 @@
 AbstractICache::flush_icache_stub_t AbstractICache::_flush_icache_stub = NULL;
 
 void AbstractICache::initialize() {
+#ifdef __plantos__
+  return;
+#else
   // Making this stub must be FIRST use of assembler
   ResourceMark rm;
 
@@ -50,9 +53,24 @@
   // which will in turn call the flush stub.  Thus we don't need an
   // explicit call to invalidate_range here.  This assumption is
   // checked in invalidate_range.
+#endif
 }
 
 void AbstractICache::call_flush_stub(address start, int lines) {
+#ifdef __plantos__
+#ifdef AMD64
+  // Match the x86 flush stub without requiring executable code during startup.
+  __asm__ volatile("mfence" ::: "memory");
+  for (int line = 0; line < lines; ++line) {
+    __asm__ volatile("clflush (%0)" :: "r"(start) : "memory");
+    start += ICache::line_size;
+  }
+  __asm__ volatile("mfence" ::: "memory");
+#else
+  (void)start;
+  (void)lines;
+#endif
+#else
   // The business with the magic number is just a little security.
   // We cannot call the flush stub when generating the flush stub
   // because it isn't there yet.  So, the stub also returns its third
@@ -63,9 +81,13 @@
   int r = (*_flush_icache_stub)(start, lines, auto_magic);
   guarantee(r == auto_magic, "flush stub routine did not execute");
   ++magic;
+#endif
 }
 
 void AbstractICache::invalidate_word(address addr) {
+#ifdef __plantos__
+  invalidate_range(addr, 4);
+#else
   // Because this is called for instruction patching on the fly, long after
   // bootstrapping, we execute the stub directly.  Account for a 4-byte word
   // spanning two cache lines by computing a start line address by rounding
@@ -81,9 +103,11 @@
   intptr_t end_line   = ((intptr_t)addr + word_size_in_bytes - 1)
                                              & ~(ICache::line_size - 1);
   (*_flush_icache_stub)((address)start_line, start_line == end_line ? 1 : 2, 0);
+#endif
 }
 
 void AbstractICache::invalidate_range(address start, int nbytes) {
+#ifndef __plantos__
   static bool firstTime = true;
   if (firstTime) {
     guarantee(start == CAST_FROM_FN_PTR(address, _flush_icache_stub),
@@ -91,6 +115,7 @@
     firstTime = false;
     return;
   }
+#endif
   if (nbytes == 0) {
     return;
   }
--- a/src/hotspot/share/runtime/os.hpp
+++ b/src/hotspot/share/runtime/os.hpp
@@ -387,7 +387,7 @@
   // are passed.
   static void   pretouch_memory(void* start, void* end, size_t page_size = vm_page_size());
 
-  enum ProtType { MEM_PROT_NONE, MEM_PROT_READ, MEM_PROT_RW, MEM_PROT_RWX };
+  enum ProtType { MEM_PROT_NONE, MEM_PROT_READ, MEM_PROT_RW, MEM_PROT_RX, MEM_PROT_RWX };
   static bool   protect_memory(char* addr, size_t bytes, ProtType prot,
                                bool is_committed = true);
